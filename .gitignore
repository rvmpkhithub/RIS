# Gradle
.gradle/
build/
app/build/

# Kotlin
.kotlin/

# Local configuration — machine-specific SDK path, plus real secrets (SMTP credentials etc.) as
# of 2026-07-14/15. Never commit. See local.properties.example for the keys and when to set them.
local.properties

# Android Studio / IntelliJ (not currently used in this project, but standard to exclude)
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Signing
*.jks
*.keystore

# Logs
*.log

# This session's debugging/verification artifacts — not app source
/screen*.png
/emulator*.log
/prompt.txt

# Runtime lock file for scheduled Claude Code tasks — machine/session-specific, not project content
.claude/scheduled_tasks.lock
