## Deferred from: code review of story 1-1-first-run-registration-compliance-gate (2026-07-09)

- NavigationBarItem empty `icon = { }` lambdas on the placeholder bottom nav (`ui/App.kt`, `MainAppPlaceholder`) may render with awkward spacing — unverified visually (no device/emulator in this environment). Deferred because this placeholder is explicitly temporary scaffold, replaced by Stories 1.2/1.3/3.1/3.2's real screens.
- No de-dup/backoff on repeated compliance re-checks across rapid relaunches (`ui/App.kt`, `domain/ComplianceGate.kt`) — every cold launch with a locked registration fires a live, unauthenticated network call with no local throttling. Deferred: this matches mechanics.md's explicit "no caching needed" design decision; revisit only if it proves to be a real operational problem (e.g. admin endpoint load complaints).

## Deferred from: code review of story 1-3-receiver-configuration (2026-07-10)

- `.catch { emit(emptyList()) }` in `ReceiverRepositoryImpl.observeReceivers()` can leave the receivers `StateFlow` stuck at `emptyList()` after a DB-flow error until all subscribers drop and reattach (`WhileSubscribed(5000)` window). Deferred: inherited verbatim from Story 1.2's `ImageRepositoryImpl.observeImages()` per Story 1.3's own explicit "mirror it exactly" instruction — both repositories share the flaw and should be fixed together in a future cross-cutting pass, not patched inconsistently one at a time.
- `ReceiverEditScreen`/`ReceiversScreen`'s empty-state and form cards use `MaterialTheme.shapes.large` (`rounded.lg`) where DESIGN.md's generic Shapes section would suggest `rounded.md` for cards. Deferred: matches the "paper card" motif already used identically in Story 1.1's `SetupScreen` and Story 1.2's `ImageLibraryScreen` empty state — a pre-existing project-wide convention question, not something introduced or that should be unilaterally changed by Story 1.3 alone.
- `AppDatabase` has no fallback Room migration path beyond `1→2→3` (no handling for a downgrade, version 0, or a skipped version). Deferred: this is the intended, correct behavior per `ARCHITECTURE-SPINE.md#AD-15` ("no destructive fallback, explicit migrations required") — Room throwing on an unhandled transition is the designed safety behavior, not a bug.
- `ReceiverEditScreen`'s `existing.phoneOrEmail.removePrefix("+91")` is a silent no-op for any legacy/foreign-formatted phone number that doesn't already start with `+91`. Deferred: currently unreachable since this app has never written a differently-formatted number (every WhatsApp receiver's `phoneOrEmail` is always written with the fixed `+91` prefix by this same screen); worth hardening only if/when real imported or legacy data enters the picture.

## Deferred from: code review of story 1-3-receiver-configuration, Task 7 round (2026-07-10)

- Minimum-4 schedule-time validation lives only in `ReceiverEditScreen`'s UI, not `ReceiverRepositoryImpl`. Deferred: consistent with this vertical's established pattern (phone/email/min-max validation are also UI-only, no domain service exists for Receiver per AD-5/AD-10's CRUD exception) — enforcing this one field at the repository layer while leaving sibling fields UI-only would be an inconsistent, out-of-scope architectural change for this patch round.
- No DB-level uniqueness constraint on `(receiverId, time)` in `receiver_schedules`. Deferred: the UI already prevents duplicate schedule times on add; a DB constraint would be pure defense-in-depth with no currently-reachable bug behind it.
- `-1` sentinel encoding for `receiverId == null` in `ReceiversTab`'s route `Saver`. Deferred: pre-existing from the original Task 5 implementation, unchanged by Task 7 — an implicit assumption (Room IDs never equal `-1`) rather than an explicit encoding, but not newly introduced here.
- Swipe-to-delete has no configured dismiss-direction restriction (either swipe direction triggers the same destructive confirm dialog). Deferred: pre-existing from Task 4, unchanged by Task 7.
- Editing a receiver reassigns new primary keys to all its `ReceiverSchedule` rows every save (delete-and-reinsert rather than diff/upsert). Deferred: a real design tradeoff, but no downstream consumer exists yet — Epic 2's `SendWorker` (which would key off schedule identity) isn't built. Revisit once Epic 2 is implemented.
- No upper bound on schedule-time count per receiver. Deferred: low value, self-inflicted-only scenario.
- `%d`/`%02d` string formatting in receiver row/edit text doesn't pin `Locale.US`/`Locale.ROOT`. Deferred: pre-existing pattern across the whole codebase, single-locale (India) deployment target per SPEC.md.
- Field `onValueChange` handlers in `ReceiverEditScreen` don't clear their corresponding error state when the user edits the field after a failed Save. Deferred: a real UX gap, but applies uniformly across the whole form (not Task-7-specific) — better addressed as its own cross-cutting polish pass, not folded into this migration-focused patch round.
- No discard-changes confirmation dialog when backing out of an in-progress receiver edit. Deferred: legitimate future UX enhancement; no AC requires it, and would need dirty-state tracking not yet built.
- DESIGN.md's receiver-row spec still describes a "trailing icon-button for delete" while EXPERIENCE.md and the actual implementation use swipe-to-delete. Deferred: pre-existing contradiction from Task 4, not introduced or touched by Task 7's doc-update pass — worth reconciling in a future documentation pass.

## Deferred from: code review of story 2-1-randomized-daily-selection (2026-07-11)

- `Transmission.sentAt` is nullable with no DB `CHECK` constraint tying `status = 'SENT'` to a non-null `sentAt`. Deferred: entirely dependent on Story 2.2's not-yet-built write path; a constraint (or defensive filter) makes more sense once that code exists and its actual failure modes are known.
- `getActiveImages()` and `getRecentlySentImageIds()` run as two independent reads with no shared transaction/consistent snapshot. Deferred: disproportionate for this single-device, single-operator app with no concurrent external writers — matches this project's established pattern of not over-engineering for concurrency that doesn't actually exist here.
- No single test carries seeded data through the full `1→5` migration chain (only `3→4` tests data preservation in isolation, `4→5` only tests schema shape). Deferred: the two existing dedicated tests already cover the distinct real risks separately; a combined chain-with-data test is nice-to-have, not clearly higher-value than what exists.
- The seeded `Random(42)` re-roll test could in principle produce different output if Kotlin's stdlib PRNG algorithm ever changed. Deferred: the test only asserts weak set-cardinality ("not all 5 draws identical"), not an exact sequence, which is extremely unlikely to break even under an algorithm change; already the more robust of the two options considered during implementation.
- `ImageRepositoryImpl.uploadImages` batches multiple file copies/inserts with no transaction wrapping the whole loop. Deferred: pre-existing code from Story 1.2, untouched by this story's diff (only `getActiveImages()` was added to this file) — out of scope for Story 2.1.
- `imageSelectionEngine` is wired into `AppContainer` but nothing calls it yet. Deferred: explicitly by design per this story's own Dev Notes (it will be called by `SendWorker` starting in Story 2.2) — normal incremental delivery, not a gap.
- Acceptance Auditor noted the code fetches `active`/`excludedIds` before computing `Z`, while Task 4's prose lists "pick Z" as step 1. Deferred/dismissed as not-a-defect by the auditor's own finding — no behavioral dependency between the two, just a statement-order observation; nothing to fix.

## Deferred from: code review of story 2-2-offline-safe-queued-delivery (2026-07-11)

- The WhatsApp request has no image-attachment field (media header/uploaded-media-id reference). Deferred: already an explicit ASSUMPTION in Story 2.2's own Task 2 text, blocked on the WhatsApp Business account being provisioned — not fixable without inventing details that don't exist yet.
- No structured logging anywhere in the delivery/dispatch stack (`DeliveryRepositoryImpl`, `SendDispatcher`, compliance early-return paths). Deferred: `ARCHITECTURE-SPINE.md`'s Consistency Conventions names a "Logger wrapper" that no story so far has actually built — a cross-cutting gap across the whole codebase, not introduced by Story 2.2.
- Secrets (`SMTP_APP_PASSWORD`, `WHATSAPP_API_TOKEN`) are plaintext `const val` fields in `AppConfig.kt`, compiled directly into the APK. Deferred: identical, pre-existing pattern to Story 1.1's `REGISTRATION_API_URL`/`COMPLIANCE_API_URL`, not a new issue.
- `SendDispatcher.attemptDelivery`'s send-then-update aren't atomic (including a discarded `AppResult` from `transmissionRepository.update(...)`) — a crash or DB-write failure between a successful send and its status update can cause a duplicate send on the next retry. Deferred: closing this needs either an idempotency key at the channel level (not available for WhatsApp/SMTP as external systems) or an outbox/2PC pattern disproportionate to this single-device, low-stakes app; worst case is one duplicate image to one receiver. The discarded-`AppResult` sub-point also matches an already-established codebase pattern (`ComplianceGate.evaluate`'s `recordCheckResult(...)` call is likewise unchecked).
- Sequential (non-concurrent) processing of receivers/retry candidates within one `SendWorker` tick. Deferred: acceptable at this app's actual scale (~10 receivers per SPEC.md), and the worst-case single-item hang is now bounded once the SMTP-timeout patch lands.
- `ExistingPeriodicWorkPolicy.KEEP` means a future change to the periodic `WorkRequest` won't apply to an already-installed build unless the unique work name changes. Deferred: a standard WorkManager gotcha, worth noting for whoever ships the next update, not actionable in the abstract right now.
- The retry pass's "receiver/image deleted since queuing" skip (Task 4) leaves that row permanently `PENDING`, never marked terminal. Deferred: already explicitly disclosed in Story 2.2's own Dev Notes/Task 4 text as "a defensive skip, not an expected path in practice."
- "Today" boundary computation in `SendDispatcher` trusts the live wall clock with no guard against the device clock being set backward. Deferred: a universal limitation of any wall-clock-scheduled system, not specific to this story.
- No DB-level uniqueness constraint enforcing one-row-per-receiver+scheduleTime+day on `transmissions`. Deferred: same root cause as the retry-pass day-boundary decision item from this review round; defense-in-depth only once that policy is decided.
- `AppConfig`'s TODO placeholder URLs are syntactically valid, so a forgotten real value fails silently at runtime rather than loudly at build/launch time. Deferred: same pre-existing pattern as Story 1.1's placeholders, not new.

## Deferred from: code review of story 3-1-delivery-dashboard (2026-07-11)

- `observeSentHistory`'s history window doesn't recompute as real time advances while the Dashboard screen stays open on the same receiver — the cutoff is only recomputed on a receiver switch. Deferred: this app's operator glances at the dashboard occasionally rather than leaving it open for days; negligible practical impact relative to the fix's complexity.
- No `LIMIT`/pagination on `observeSentHistory` — the query loads the full result set on every Room invalidation. Deferred: disproportionate for this app's actual scale (~10 receivers, a 30-day window at 2–5 images/day tops out around 60–150 rows per receiver).
- `AsyncImage` has no `error`/`placeholder` composable configured for a thumbnail whose file no longer resolves. Deferred: matches the exact pre-existing convention already used by `ImageLibraryScreen`'s `ImageGridItem`, a cross-cutting gap across both screens, not introduced by this story.
- `AsyncImage`'s `contentDescription = null` marks the thumbnail as fully decorative to accessibility services. Deferred: same pre-existing convention as `ImageGridItem`, cross-cutting.
- All user-facing strings are hardcoded rather than extracted to Android string resources. Deferred: pre-existing codebase-wide convention (`ReceiversScreen`/`ImageLibraryScreen`'s empty-state copy is inline the same way), not specific to this story.
- Passing the entire `AppContainer` into the leaf `DashboardEntryRow` composable is a wider dependency surface than the row actually needs. Deferred: legitimate architecture nitpick with no functional impact; reasonable future cleanup, not urgent.
- Repeated `receivers!!`/`history!!` non-null assertions instead of a single local `val` or a sealed UI-state model. Deferred: each `!!` is directly guarded by the enclosing branch in the same scope, safe in practice — a style nitpick, not a functional bug.
- `DashboardViewModelTest`'s "switching receiver" test carries a comment acknowledging it depends on `stateIn`/`WhileSubscribed` timing nuances. Deferred: already documented inline, the underlying behavior is well-understood and correctly reasoned about.
- No explicit re-emission of `null`/loading when switching between two already-selected (valid) receivers. Deferred: local Room queries emit near-instantly, making any cross-receiver flash imperceptible in practice; forcing an explicit null-flash would trade an unobserved risk for a guaranteed loading-spinner flicker on every switch.

## Deferred from: code review of story 3-2-configurable-retention-purge (2026-07-11)

- `.catch { emit(fallback) }` on a Room-backed `Flow` completes that flow after one error rather than resuming future emissions — a pre-existing, codebase-wide convention (`ComplianceRepositoryImpl` since Story 1.1, `TransmissionRepositoryImpl.observeSentHistory` since Story 3.1). Deferred: Task 6's use of `combine()` over two such flows means a single transient `retention_settings` read error could now permanently freeze the Dashboard's history across all future receiver switches (previously, `flatMapLatest` on `selectedReceiverId` alone provided a natural recovery path) — flagged for any future story building further on `DashboardViewModel`, but disproportionate to redesign now given a trivial single-row query essentially never throws in practice.
- The purge deletes by `createdAt` with no `status` filter, so a still-`PENDING` transmission could theoretically be purged if the operator sets an extremely low retention value that races against Story 2.2's own "~1 day" auto-terminate guarantee. Deferred: already reasoned about and deliberately accepted in this story's own Dev Notes; requires a deliberate, extreme operator setting (e.g. 1 day) to ever trigger.
- `ExistingPeriodicWorkPolicy.KEEP` locks `RetentionPurgeWorker`'s interval in at first-install forever for an already-installed app. Deferred: identical, already-documented trade-off inherited from `SendWorker` (Story 2.2's own review), not introduced by this story.
- No confirmation or warning is shown when an operator lowers the retention period, silently condemning the difference in history to deletion on the next purge. Deferred: a real UX gap, but no AC or design doc calls for it — scope creep beyond this story's own explicit boundary, the same discipline Story 3.1 held to for its "no purge-now button" decision.

## Deferred from: code review of story 1-2-image-library-management (2026-07-12)

- No image-delete capability exists anywhere in the app — uploaded images (and their files) persist forever, with no user-facing way to reclaim storage or remove a mistaken upload. Deferred: the story's own ACs only ever asked for upload + active/inactive toggle, never delete — a real, permanent-accumulation product gap worth a future story's consideration, not a Story 1.2 implementation bug.
- `ImageLibraryViewModel.onImagesPicked`/`onToggleActive` both silently discard the repository's returned `AppResult`, with no error state ever surfaced in the UI. Deferred: the screen's `StateFlow<List<Image>>` is fully reactive, so a failed operation simply doesn't change what's on screen rather than leaving it in a wrong state (an implicit, if unfriendly, failure signal) — no AC/design doc calls for explicit error UI, and building that is a UI feature addition beyond a bug fix.
- `setActive` silently no-ops (0 rows affected) if the target id no longer exists. Deferred: currently unreachable in production, since nothing in this app ever deletes an `Image` row — ids can't go stale today. Revisit if/when delete is ever implemented.
- No upload-in-progress/loading indicator during a multi-image upload. Deferred: real UX gap, but no AC/design doc calls for it.
- No file-size or upload-count cap. Deferred: disproportionate for this single-operator, low-volume app; no spec calls for a limit.
- The stored file extension defaults to `.jpg` on a failed/missing MIME lookup without validating actual file content. Deferred: self-assessed as "cosmetic today" since Coil sniffs by content, not extension — a latent landmine for hypothetical future extension-based logic, not a current bug.
- `Image.filePath` has no DB index/uniqueness constraint, and `observeAll()` has no pagination. Deferred: proportionate for this app's expected scale (a personal image library); revisit if the library ever grows large enough to matter.
- The instrumented `ImageLibraryScreenTest` only exercises the empty-state case — no grid/toggle/picker-launch coverage. Deferred: expanding Compose UI coverage for this screen is valuable but a substantial addition beyond a catch-up review's scope. This review did discover the story's own "won't execute in this sandbox" disclosure was stale — the existing test (and the rest of the instrumented suite) now runs cleanly in this environment.

## Deferred from: code review of story 1-4-image-tagging-list-view (2026-07-13)

- Active toggle on the Image Detail screen commits immediately to the DB while title/description edits are buffered and only committed on Save — backing out after editing text (with or without touching the toggle) silently discards the text but keeps the toggle change, with no discard-confirmation. Deferred: explicit operator decision — acceptable behavior for this internal single-operator tool, no discard-guard needed.
- `ImageRepositoryImpl.updateImageDetails` ignores the DAO's affected-row count, so updating a since-deleted image's id reports `Success` though nothing changed. Deferred: unreachable today — the app has no image-delete feature yet, so an image id can't go stale (same class of issue already deferred in Story 1.2's review for `setActive`).
- A stale in-flight save callback race: pressing back mid-save on the Image Detail screen and re-entering Detail before the stale `onResult` fires can force an unexpected route change back to List. Deferred: narrow timing window, no currently-exercised path.
- Blank-to-`null` trimming for image title/description is enforced only in `ImageRepositoryImpl`, not at the `ImageDao`/entity boundary. Deferred: single call site today, matches this codebase's existing single-layer-normalization convention (compare `ReceiverEditScreen`'s equivalent UI-layer-only trimming).
- `ImageLibraryViewModel`'s shared `_isSaving` flag now spans both the Image Library (list) and Image Detail screens. Deferred: no cross-screen conflict is reachable today since only the Detail screen currently drives it.

## Deferred from: code review of story 2-4-single-image-selection (2026-07-13)

- A receiver with no schedule of its own AND an empty/unconfigured master schedule shows "Uses master schedule" in the Receivers list with no warning that it will never actually receive anything — the existing `isNotEmpty() && size < MIN_SCHEDULE_TIMES` warning condition doesn't catch this combination. Deferred: pre-existing, out of scope for this story — Story 2.4's diff only removed the image-count-range text suffix from the row; the warning condition itself is unchanged code from Story 2.3.

## Deferred from: code review of story 2-3-master-schedule-fallback (2026-07-13)

- `MIN_SCHEDULE_TIMES` (value `4`) is duplicated across three files — `ReceiverEditScreen.kt`, `SettingsScreen.kt` (as `MIN_MASTER_SCHEDULE_TIMES`), and `MasterScheduleRepositoryImpl.kt`. Deferred: each site's doc comment explains this is intentional (a data-layer class must not depend on a UI-layer constant), consistent with this codebase's established layering discipline — but it remains a manual-sync risk if the value ever changes, worth a shared-constants-file pass if it ever does.

## Deferred from: code review of story 1-5-tag-on-upload (2026-07-13)

- A device rotation in the narrow window between a successful upload and the Room `images` Flow's re-emission of the new row drops `ImagesRoute.Detail.preloaded`, falls through to `images.find` (which hasn't caught up), and silently bounces to List even though the upload succeeded (`hasLoaded` is already `true`, so `ImageDetailScreen` treats it as "not found" rather than "still loading"). Deferred: sub-second window, no data loss (image is safely persisted, only that session's immediate tagging opportunity is lost — recoverable via "View"), consistent with this codebase's tolerance for similarly narrow, non-data-losing races.
- `ImagesTab.kt`'s actual routing behavior (upload → Detail navigation, `BackHandler`, the `preloaded` fallback) has no automated Compose UI test — only `ImagesRouteSaverTest`'s pure string-serialization tests exist. Deferred: directly and thoroughly live-verified in this story's own Task 6 instead; a Compose UI test would need to mock `ActivityResultLauncher` picker results, which is awkward to author — a future improvement, not a blocking gap.
