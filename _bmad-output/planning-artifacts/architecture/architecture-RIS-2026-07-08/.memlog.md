---
scope: Full system architecture spine for the scheduled image-distributor app (spec-image-distributor-app): sideloaded Android tablet/phone app for one operator, local Postgres, WhatsApp Business API + Gmail SMTP delivery, compliance/licensing gate
purpose: Build substrate for direct implementation by the assistant - not a human-facing document
altitude: product (whole small app, single deliverable, no epic decomposition yet)
updated: 2026-07-09T11:03
---

- (decision) Working mode: Coaching path
- (decision) Purpose/audience: spine + docs are a build substrate for the assistant to implement directly, not a human-facing deliverable
- (constraint) Distribution confirmed: operator sends the app package to his friend over WhatsApp; friend downloads and installs it directly on his tablet. Confirms Android as the target platform (APK sideload)
- (decision) Storage decision: SQLite on-device (via Room) replaces the spec's 'local Postgres' constraint - no server process, no network dependency, comfortably handles ~500 images (as files, metadata in DB) and 30 days of transmission logs (~thousands of rows). Overrides SPEC.md constraint; must sync back to spec at handoff
- (decision) Paradigm: Layered MVVM (Compose UI -> ViewModel -> Repository -> data sources), Google's official Android App Architecture guidance. Confirmed by user
- (decision) No backend server: the Android app calls the compliance API, WhatsApp Business Cloud API, and Gmail SMTP directly from the device. Trade-off named and accepted: embeds a long-lived WhatsApp access token in the sideloaded APK, acceptable given single trusted private install. Confirmed by user
- (decision) DI: manual DI via a small AppContainer, not Hilt - avoids annotation-processing build complexity for a single-operator, few-screen app. Confirmed by user
- (version) Kotlin 2.4.0; Jetpack Compose BOM 2026.04.01; Android Gradle Plugin 9.2.0; Room 2.6.x; WorkManager 2.11.2; Retrofit + OkHttp 5.0; Jakarta Mail for Gmail SMTP. All verified current as of 2026-07 via web search
- (decision) Background reliability: request 'ignore battery optimizations' exemption once at setup; use WorkManager periodic + expedited work for scheduled sends and retries. Confirmed by user
- (decision) App updates: fully manual - operator rebuilds and resends a new APK over WhatsApp; friend (installer) manually reinstalls. No in-app update-check mechanism. Confirmed by user
- (event) Spine distilled: 10 ADs, stack table, ERD, dependency diagram, source tree, capability map, deferred list written to ARCHITECTURE-SPINE.md
- (event) Reviewer gate ran: lint clean; reconcile=minor gaps (CAP-4/CAP-6 missing mechanics.md citations, 3-retry/no-backfill/registration-lock/retention-default not bridged); rubric=critical self-contradiction (COMPLIANCE_STATE entity looks like the forbidden 2-day cache) + migration strategy + image storage gaps; versions=Room and Compose BOM stale, Retrofit/OkHttp pairing inconsistent, Gmail auth method unspecified, WorkManager version unconfirmed; adversarial=6 divergence pairs found, most serious being the same compliance-cache contradiction plus AD-1/AD-5 call-path ambiguity, Transmission row lifecycle, and WorkManager fan-out shape all undecided
- (decision) Fix (critical): COMPLIANCE_STATE.isCompliant/lastCheckedAt is a display-only cache for offline UI status; ComplianceGate always performs a live check when online and never consults this cached value to decide whether to halt - resolves the contradiction with mechanics.md's 'no caching needed' rule
- (decision) Fix: resolve AD-1/AD-5 conflict - ViewModels/Workers may call Repository directly for simple CRUD, but must call domain services (ImageSelectionEngine, ComplianceGate, RetentionPolicy) for anything those own; domain services take Repository dependencies via constructor injection and call them internally, they are not pure functions requiring pre-fetched args
- (decision) Fix: Transmission is one row per queued send, updated in place across retries (status/attemptCount mutate on the same row); sentAt is set only when status becomes SENT. Prevents double-counted dashboard entries
- (decision) Fix: single periodic scanning Worker (not per-receiver WorkManager requests) - runs on a short fixed interval, checks each receiver's configured scheduleTime against current time, dispatches any due-and-not-yet-sent-today receivers. Avoids managing N work requests and WorkManager's inexact timing
- (decision) Fix: image files stored in app-private internal storage (context.filesDir/images/), referenced by relative filename in Image.filePath - no MediaStore/content-URI, no storage permission needed
- (decision) Fix: Receiver.scheduleTime stored as minutes-since-midnight (Int, device local time); RetentionSetting and ComplianceState are singleton tables seeded with a fixed id=1 row at DB creation, always queried/updated at id=1, no other insert path
- (version) Fix: Room updated to 2.8.x stable line (2.6.x was stale; Room 3.0 KMP/coroutines-only alpha noted but deferred). Compose BOM updated to 2026.06.01. Retrofit set to 3.0.0 using its bundled default OkHttp (4.12) rather than force-pairing with OkHttp 5.0. WorkManager softened to 2.10.x pending patch-level confirmation at build time. Gmail SMTP auth: App Password chosen as the simple default; OAuth2/XOAUTH2 migration deferred in case Google further restricts app passwords
- (decision) Fix: Capability Map rows for CAP-4 and CAP-6 now cite mechanics.md and name the 3-retry cap, no-backfill rule, and permanent registration lock explicitly; CAP-8 row states the 30-day default retention
- (event) Fixes applied: AD-11..AD-15 added (compliance-cache clarification, scanning worker, transmission row lifecycle, image storage, schema migrations); AD-5 amended to resolve AD-1 conflict; stack versions corrected (Room 2.8.x, Compose BOM 2026.06.01, Retrofit 3.0.0/OkHttp 4.12, WorkManager 2.10.x, Gmail App Password); Capability Map bridged to mechanics.md for CAP-4/CAP-6/CAP-8; conventions table gained scheduleTime format and singleton-table semantics. Lint re-run: clean. No open questions or [ASSUMPTION] tags remain - nothing to triage
- (event) spine finalized
- (decision) AD-11 amended during Story 1.1 code review: cache use is asymmetric - ComplianceGate still never fabricates a false 'compliant' proceed decision from cache, but on a Failure (unreachable) it now checks the cache for a confirmed prior non-compliant result and extends the halt if found. Closes a real bypass (offline relaunch defeated an explicit halt). Resolution to a decision-needed code review finding, user did not respond within timeout so the recommended option (make it sticky) was applied, consistent with this session's established pattern for unanswered recommendations
